name: ff_gaming_hub
description: FF Gaming Hub Premium App.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_mobile_ads: ^5.0.0
  installed_apps: 2.0.1
  device_info_plus: ^10.0.0
  battery_plus: ^6.0.0
  dart_ping: ^9.0.1
  
  
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
  uses-material-design: true
  
